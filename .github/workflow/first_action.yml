name: first workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
     - name: Print greeting
       run: echo "Hello world"
     - name: print bye
       run: echo "Done - bye"
     - name: date
       run:  date
     - name: list of Directories
       run: ls -al
       
